name: Deploy Docs Caller

on:
  push:
    branches:
      - '**'  # Trigger on push to any branch (so this file can be the same in all release branches)

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  call-shared:
    uses: jjainschigg-r/k0rdent-oss-docs/.github/workflows/deploy-docs.yml@main
